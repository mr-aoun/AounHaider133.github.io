/*Write a C Program to create the data for some students (roll, name, mark1, mark2, mark3) and then find the total marks for each student and average mark of each student.*/
#include <stdio.h>
struct student {
int total;
float average;
};
int main()  {
    struct student roll,name,mark1,mark2,mark3,a,b;
    printf("Enter roll number:");
    scanf("%d",&roll.total);
    printf("Enter name:");
    scanf("%c",&name.total);
    printf("Enter mark 1:");
    scanf("%d",mark1.total);
    printf("Enter mark 2:");
    scanf("%d",mark2.total);
    printf("Enter mark 3:");
    scanf("%d",mark3.total);
b.total=(mark1+mark2);
a.average=(mark4.total)/3;
    printf("Total =%d\n",b.total);
    printf("Average=%f\n",a.average);
 return 0;
}
