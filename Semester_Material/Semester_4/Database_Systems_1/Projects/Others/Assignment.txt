import javafx.application.Application;
import javafx.geometry.Insets;
import javafx.geometry.Pos;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextField;
import javafx.scene.layout.BorderPane;
import javafx.scene.layout.GridPane;
import javafx.scene.text.Font;
import javafx.stage.Stage;

public class Sample extends Application {
    // Connection parameters for MySQL database
    private static final String DB_URL = "jdbc:mysql://localhost:3306/record";
    private static final String DB_USER = "root";
    private static final String DB_PASS = "talha";

    // Main method to launch the application
    public static void main(String[] args) {
        launch(args);
    }

    // Start method to create and display the GUI
    @Override
    public void start(Stage primaryStage) throws Exception {
        // Create a border pane for the GUI
        BorderPane root = new BorderPane();

        // Create a label for the header
        Label header = new Label("Welcome to home page");
        header.setFont(new Font("Arial", 20));

        // Create a menu button
        Button menuButton = new Button("Menu");
        menuButton.setDisable(true); // Button is clickable but does nothing

        // Create a label for the footer
        Label footer = new Label("© 2023 My home page. All rights reserved.");

        // Create a grid pane for the sign up page
        GridPane signUpGrid = new GridPane();
        signUpGrid.setAlignment(Pos.CENTER);
        signUpGrid.setHgap(10);
        signUpGrid.setVgap(10);
        signUpGrid.setPadding(new Insets(25, 25, 25, 25));

        // Create text fields and labels for the sign up page
        Label signUpHeader = new Label("Sign Up");
        signUpHeader.setFont(new Font("Arial", 18));
        Label signUpNameLabel = new Label("Name:");
        TextField signUpNameField = new TextField();
        Label signUpAgeLabel = new Label("Age:");
        TextField signUpAgeField = new TextField();
        Label signUpAddressLabel = new Label("Address:");
        TextField signUpAddressField = new TextField();
        Label signUpUsernameLabel = new Label("Username:");
        TextField signUpUsernameField = new TextField();
        Label signUpPasswordLabel = new Label("Password:");
        PasswordField signUpPasswordField = new PasswordField();
        Label signUpConfirmPasswordLabel = new Label("Confirm Password:");
        PasswordField signUpConfirmPasswordField = new PasswordField();
        Label signUpErrorLabel = new Label("");
        signUpErrorLabel.setStyle("-fx-text-fill: red;");
        Button signUpButton = new Button("Sign Up");
        signUpButton.setOnAction(e -> {
           // Validate the password
            String password = signUpPasswordField.getText();
            String confirmPassword = signUpConfirmPasswordField.getText();
            boolean validPassword = validatePassword(password);
           if (!validPassword) {
                signUpErrorLabel.setText("Invalid password. Password must be at least 8 characters with at least one capital, one numeral, and one special character.");
                return;
          }
        });
    }
}